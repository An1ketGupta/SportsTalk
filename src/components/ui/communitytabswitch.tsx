import { Button } from "./button";

export default function CommunityTabSwticher(){
    return <div className="flex w-full">
        <Button variant="ghost" size={"lg"} className="w-full">For You</Button>
        <Button variant="ghost" size={"lg"} className="w-full">For You</Button>
    </div>
}